@using E_Agenda.WebApp.Models

@model ViewContactsViewModel

@{
    ViewBag.Title = "Lista de Contatos";
    Layout = "_Layout";
}

<p>
    <a class="btn btn-primary px-4" href="/contacts/register">
        <i class="bi bi-plus"></i>
        Cadastrar Novo
    </a>
</p>

<hr />

<div class="d-flex flex-wrap gap-2">
    @foreach (var item in Model.Contacts)
    {
        <div class="col-12 col-md-3">
            <div class="card border-0">
                <div class="card-body">
                    <h5 class="card-title">Nome: @item.Name</h5>
                    <p class="card-text"><span class="fw-semibold">Email:</span> @item.Email</p>

                    <span class="d-flex gap-1 justify-content-end mt-4">
                        <a href="/contacts/details/@item.Id" class="btn btn-secondary rounded-circle opcao-card">
                            <i class="bi bi-search"></i>
                        </a>
                        <a href="/contacts/edit/@item.Id" class="btn btn-primary rounded-circle opcao-card">
                            <i class="bi bi-pencil-square"></i>
                        </a>
                        <a href="/contacts/delete/@item.Id" class="btn btn-danger rounded-circle opcao-card">
                            <i class="bi bi-trash"></i>
                        </a>
                    </span>
                </div>
            </div>
        </div>
    }
</div>
